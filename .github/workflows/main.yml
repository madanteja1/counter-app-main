name: hello world
on:
   push:
   branches : [main]

jobs:
  build:
    runs-on:ubuntu:lastest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v2
